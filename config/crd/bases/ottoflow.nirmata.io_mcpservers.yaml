---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: mcpservers.ottoflow.nirmata.io
spec:
  group: ottoflow.nirmata.io
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    shortNames:
    - mcpserver
    - mcpservers
    singular: mcpserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MCPServer is the Schema for the mcpservers API
          MCPServer defines an MCP (Model Context Protocol) server configuration
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer
            properties:
              auth:
                description: Auth defines authentication configuration
                properties:
                  oauth2:
                    description: |-
                      OAuth2 config for OAuth 2.0/2.1 client credentials flow (machine-to-machine)
                      Used when type is oauth2
                    properties:
                      clientCredentialsRef:
                        description: |-
                          ClientCredentialsRef references a Secret with keys "client_id" and "client_secret"
                          Namespace defaults to MCPServer namespace
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        type: object
                      clientId:
                        description: ClientID is the OAuth2 client ID (alternative
                          to ClientCredentialsRef)
                        type: string
                      clientSecretRef:
                        description: |-
                          ClientSecretRef references a Secret for credentials
                          For client credentials flow: use key "client_secret" (with ClientID above), or
                          use ClientCredentialsRef for both client_id and client_secret
                        properties:
                          key:
                            description: Key is the key in the Secret
                            type: string
                          name:
                            description: Name is the name of the Secret
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the Secret. Defaults to the namespace of the
                              resource that references this Secret (MCPServer, Workflow, etc.).
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      scopes:
                        description: Scopes are optional OAuth2 scopes (space-separated)
                        items:
                          type: string
                        type: array
                      tokenURL:
                        description: TokenURL is the OAuth2 token endpoint (e.g.,
                          https://auth.example.com/oauth/token)
                        type: string
                    required:
                    - tokenURL
                    type: object
                  secretRef:
                    description: |-
                      SecretRef references a Kubernetes Secret containing credentials.
                      Required for bearer, apiKey, and basic auth. For bearer/apiKey the secret key holds the token.
                      For basic auth the secret must contain keys "username" and "password".
                    properties:
                      key:
                        description: Key is the key in the Secret
                        type: string
                      name:
                        description: Name is the name of the Secret
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the Secret. Defaults to the namespace of the
                          resource that references this Secret (MCPServer, Workflow, etc.).
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  type:
                    default: none
                    description: |-
                      Type is the authentication type
                      Options: none, bearer, apiKey, basic, oauth2
                    enum:
                    - none
                    - bearer
                    - apiKey
                    - basic
                    - oauth2
                    type: string
                type: object
              env:
                description: Env is a list of environment variables to set for the
                  MCP server process
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              timeout:
                description: Timeout is the connection timeout (e.g., "30s", "5m")
                type: string
              transport:
                description: Transport defines how to connect to the MCP server
                properties:
                  address:
                    description: Address is the server address (for http/sse)
                    type: string
                  command:
                    description: Command is the command to execute (for stdio)
                    items:
                      type: string
                    type: array
                  headers:
                    additionalProperties:
                      type: string
                    description: Headers are HTTP headers (for http/sse)
                    type: object
                  type:
                    description: |-
                      Type is the transport type
                      Options: stdio, http, sse
                    enum:
                    - stdio
                    - http
                    - sse
                    type: string
                required:
                - type
                type: object
            required:
            - transport
            type: object
          status:
            description: MCPServerStatus defines the observed state of MCPServer
            properties:
              availableTools:
                description: AvailableTools is a list of tools available from this
                  MCP server
                items:
                  type: string
                type: array
              lastConnected:
                description: LastConnected is when the server was last successfully
                  connected
                format: date-time
                type: string
              message:
                description: Message provides additional information about the server
                  status
                type: string
              phase:
                allOf:
                - enum:
                  - Ready
                  - NotReady
                - enum:
                  - Ready
                  - NotReady
                description: Phase represents the current phase of the MCPServer
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
